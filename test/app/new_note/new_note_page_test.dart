import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:note/app/new_note/new_note_page.dart';

void main() {
  testWidgets('NewNotePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(NewNotePage(title: 'NewNote')));
    //  final titleFinder = find.text('NewNote');
    //  expect(titleFinder, findsOneWidget);
  });
}
